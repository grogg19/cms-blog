{% include('header.html.twig') %}
?>
<div class="dx-box-1 pb-100 bg-grey-6">
    <div class="container">
        <div class="row vertical-gap md-gap">
            <div class="col-lg-12">
                {% if not pageParameters is empty %}
                {{ content|raw }}
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% include('footer.html.twig') %}